name: Run Expense Pipeline

on:
  push:
    paths:
      - 'statements/**'  # Triggers automatically whenever you upload or update files in the statements folder

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v3

    - name: Set up Python Environment
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Required Libraries
      run: |
        python -m pip install --upgrade pip
        pip install pandas google-generativeai gspread oauth2client

    - name: Execute Expense Tracker Script
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        GOOGLE_CREDS_JSON: ${{ secrets.GOOGLE_CREDS_JSON }}
      run: python tracker.py
